#include <stdio.h>

#define LEN 50

struct endereco{
	char rua[LEN];
	char cidade_estado_cep[LEN];
};

struct estudante{
	char id[10];
	int idade;
	struct endereco casa;
	struct endereco escola;
};

struct estudante pessoa;


